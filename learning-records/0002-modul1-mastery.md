# Modul 1 Mastery: Variables, Types, and Basic Arithmetic

Pengguna telah menyelesaikan Modul 1 (variabel, tipe data, konversi tipe eksplisit, dan string template) dengan lancar.

## Evidence
- `latihan1.kt`: Penggunaan `val` dan string template `$namaLengkap` dsb.
- `latihan2.kt`: Perhitungan aritmatika diskon dan total bayar.
- `latihan3.kt`: Berhasil melakukan parsing string ke integer secara eksplisit menggunakan `.toInt()`.

## Notes for Next Sessions
- Perlu menekankan pembagian integer vs floating-point (`Int / 100` vs `Double`) untuk mencegah pembulatan tak terduga dalam perhitungan keuangan/diskon di masa mendatang.
- Siap lanjut ke Modul 2: Control Flow (`if-else` sebagai ekspresi, `when` expression, `for`/`while` loops, dan ranges).
