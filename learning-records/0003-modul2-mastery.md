# Modul 2 Mastery: Control Flow, Expressions, and Loops

Pengguna telah menguasai konsep Control Flow di Kotlin: `if-else` sebagai expression, `when` expression dengan multi-value dan ranges, serta `for` loop dengan `downTo`, `step`, dan nested loop.

## Evidence
- `latihan1.kt`: Penggunaan `if-else` expression bertingkat untuk assigning value ke `val levelPengguna`.
- `latihan2.kt`: Penggunaan `when` expression dengan `4,5` (multi-match), `in 6..10` (range match), dan `else`.
- `latihan3.kt`: Penggunaan `15 downTo 1 step 2` dan nested loop `1..i` untuk pola segitiga bintang.

## Next Steps
- Siap lanjut ke Modul 3: Functions (Default & Named arguments, Single-expression functions, Unit, Vararg, dan pengenalan Lambda dasar).
