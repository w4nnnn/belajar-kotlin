# Modul 3 Mastery: Functions in Kotlin

Pengguna telah menguasai konsep Functions di Kotlin: Single-Expression Functions, Default & Named Arguments, serta `vararg` dengan penanganan edge cases.

## Evidence
- `latihan1.kt`: Penerapan Single-Expression Functions (`=`) dengan type inference / eksplisit return type pada 3 fungsi (`konversiCelciusKeFahrenheit`, `hitungDiskon`, `isBilanganGenap`).
- `latihan2.kt`: Deklarasi fungsi dengan Default Arguments (`teksTombolPositif`, `teksTombolNegatif`, `bisaDibatalkan`), pemanggilan pertama dengan argumen posisi minimal, serta pemanggilan kedua dengan Named Arguments secara spesifik.
- `latihan3.kt`: Penerapan `vararg` untuk agregasi data dinamis serta penanganan edge case `isEmpty()` agar pembagian nol tidak menghasilkan `NaN`.

## Next Steps
- Siap lanjut ke Modul 4 (Collections & Functional Programming dasar seperti List, Map, Set, serta operasi `map`, `filter`, `forEach`).
